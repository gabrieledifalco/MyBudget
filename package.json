{
  "name": "mybudget",
  "version": "0.1.0",
  "private": true,
  "description": "Financial Planner App - monolite con frontend e backend separati",
  "type": "module",
  "workspaces": [
    "app/backend",
    "app/frontend"
  ],
  "scripts": {
    "dev": "concurrently -n BE,FE -c blue,magenta \"npm:dev:be\" \"npm:dev:fe\"",
    "dev:be": "npm run dev --workspace @mybudget/backend",
    "dev:fe": "npm run dev --workspace @mybudget/frontend",
    "build": "npm run build --workspace @mybudget/frontend",
    "start": "npm run start --workspace @mybudget/backend",
    "db:migrate": "npm run db:migrate --workspace @mybudget/backend",
    "db:seed": "npm run db:seed --workspace @mybudget/backend",
    "db:studio": "npm run db:studio --workspace @mybudget/backend",
    "lint": "npm run lint --workspaces --if-present",
    "setup": "npm install && npm run db:migrate && npm run db:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
