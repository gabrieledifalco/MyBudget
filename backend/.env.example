# Copia in backend/.env e adatta i valori
PORT=4000
NODE_ENV=development

# SQLite in locale, sostituibile con PostgreSQL cambiando anche il provider in prisma/schema.prisma
DATABASE_URL="file:./dev.db"

JWT_SECRET="cambia-questa-stringa"
JWT_EXPIRES_IN="7d"

CORS_ORIGIN="http://localhost:5173"
